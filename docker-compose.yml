services:
  schola-page:
    image: ${IMAGE_NAME:-schola-page:latest}
    container_name: ${DOCKER_NAME:-schola-page}
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
